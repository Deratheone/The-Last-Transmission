# Room 7 / Stage 7 — The Final Transmission

## Story beat

The Communication Core is almost restored, but it rejects every individual access code. The smartboard explains that the nodes were never the final locks; they were the way to reconstruct the missing master key.

## Participant task

Enter the six recovered fragments in the final reconstruction order:

`TIMEIS-NOTDIS-TANCEI-TSMEMO-RY2042-EVA!!!`

The page rejects a single code or an incorrectly formatted sequence.

## Expected result

The recovered log auto-scrolls through EVA's first exchange with the unknown signal. It reveals that the sender — called the Creator, Observer, and Overseer — is EVA forty-three years in the future.

The core ends with:

`GLOBAL COMMUNICATION NETWORK: ONLINE`  
`WELCOME BACK, EVA.`

## Files and hardware

- `index.html` — final smartboard page.
- Hardware: the Room 7 smartboard or any browser-capable computer.

## Facilitator note

Do not prefill the master key. Allow the full auto-scrolling log to complete before ending the event.
